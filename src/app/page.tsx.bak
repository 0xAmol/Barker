export default function Home() {
  return (
    <div style={{
      minHeight: "100vh",
      background: "#0c0b09",
      color: "#ece6d8",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      fontFamily: "system-ui",
    }}>
      <div style={{ textAlign: "center" }}>
        <h1 style={{ fontSize: 48, marginBottom: 8 }}>Barker</h1>
        <p style={{ color: "#6e675d" }}>AI Salesman — running on port 3001</p>
        <p style={{ color: "#6e675d", marginTop: 16, fontSize: 14 }}>
          API routes: /api/agent/create · /api/leads/capture · /api/webhook/sms
        </p>
      </div>
    </div>
  );
}
